name: flightpath_mobile
description: FlightPath — your flights, mapped.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  maplibre_gl: ^0.20.0
  fl_chart: ^0.68.0
  latlong2: ^0.9.0
  great_circle_distance_calculator: ^1.0.0
  intl: ^0.19.0
  provider: ^6.1.0
  cupertino_icons: ^1.0.8
  # 6.4.x requires a newer Flutter Gradle API than the CI-pinned Flutter 3.24.
  app_links: 6.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
